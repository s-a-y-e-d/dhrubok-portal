"use client";

import { useMutation, useQuery } from "convex/react";
import { useState, type FormEvent } from "react";
import { api } from "@convex/_generated/api";
import type { Id } from "@convex/_generated/dataModel";
import { PortalPageState } from "./PortalPageState";

export function ContentEditor({ locale, role, initialTab = "materials" }: { locale: "bn" | "en"; role: "owner" | "teacher"; initialTab?: "materials" | "notices" }) {
  const bn = locale === "bn"; const [tab, setTab] = useState(initialTab); const ownerScopes = useQuery(api.academics.options.contentScopes, role === "owner" ? {} : "skip"); const teacherScopes = useQuery(api.academics.readModels.teacherAssignedBatches, role === "teacher" ? {} : "skip"); const [status, setStatus] = useState<"draft" | "published">("draft"); const materials = useQuery(api.materials.functions.listManaged, tab === "materials" ? { status } : "skip"); const notices = useQuery(api.notices.functions.listManaged, tab === "notices" ? { status } : "skip");
  const createMaterial = useMutation(api.materials.functions.create); const uploadUrl = useMutation(api.materials.functions.generateUploadUrl); const publishMaterial = useMutation(api.materials.functions.publish); const createNotice = useMutation(api.notices.functions.create); const publishNotice = useMutation(api.notices.functions.publish); const [previewId, setPreviewId] = useState<Id<"notices"> | "">(""); const preview = useQuery(api.notices.functions.previewSms, previewId ? { noticeId: previewId } : "skip"); const [busy, setBusy] = useState(false); const [message, setMessage] = useState<string | null>(null);
  const courses = role === "owner" ? ownerScopes?.courses ?? [] : [...new Map((teacherScopes ?? []).map((scope) => [scope.courseId, { courseId: scope.courseId, nameBn: scope.courseNameBn, nameEn: scope.courseNameEn }])).values()]; const batches = role === "owner" ? ownerScopes?.batches ?? [] : (teacherScopes ?? []).map((scope) => ({ batchId: scope.batchId, courseId: scope.courseId, nameBn: scope.nameBn, nameEn: scope.nameEn }));
  if ((role === "owner" && ownerScopes === undefined) || (role === "teacher" && teacherScopes === undefined)) return <PortalPageState state="loading" locale={locale} />;
  async function execute(work: () => Promise<unknown>, success: string) { setBusy(true); setMessage(null); try { await work(); setMessage(success); return true; } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Operation failed"); return false; } finally { setBusy(false); } }
  async function submitMaterial(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const data = new FormData(form); const kind = data.get("kind") as "file" | "link" | "text"; const courseId = String(data.get("courseId")) as Id<"courses">; const batchId = String(data.get("batchId") || "") as Id<"batches">; let storageId: Id<"_storage"> | undefined; let fileName: string | undefined; if (kind === "file") { const file = data.get("file"); if (!(file instanceof File) || !file.size) { setMessage(bn ? "ফাইল নির্বাচন করুন।" : "Select a file."); return; } setBusy(true); const url = await uploadUrl({}); const response = await fetch(url, { method: "POST", headers: { "Content-Type": file.type }, body: file }); if (!response.ok) { setBusy(false); setMessage("Upload failed"); return; } storageId = (await response.json()).storageId as Id<"_storage">; fileName = file.name; }
    await execute(async () => { await createMaterial({ courseId, batchId: batchId || undefined, titleBn: String(data.get("titleBn")), titleEn: String(data.get("titleEn")), descriptionBn: String(data.get("descriptionBn")), descriptionEn: String(data.get("descriptionEn")), kind, storageId, fileName, externalUrl: kind === "link" ? String(data.get("externalUrl")) : undefined, visibility: batchId ? "batch" : "course", publish: data.get("publish") === "on" }); form.reset(); }, bn ? "ম্যাটেরিয়াল সংরক্ষিত হয়েছে।" : "Material saved."); }
  async function submitNotice(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const data = new FormData(form); const audienceType = data.get("audienceType") as "public" | "all_students" | "course" | "batch"; const sendSms = data.get("sendSms") === "on"; await execute(async () => { await createNotice({ titleBn: String(data.get("titleBn")), titleEn: String(data.get("titleEn")), bodyBn: String(data.get("bodyBn")), bodyEn: String(data.get("bodyEn")), audienceType, courseId: audienceType === "course" ? String(data.get("courseId")) as Id<"courses"> : undefined, batchId: audienceType === "batch" ? String(data.get("batchId")) as Id<"batches"> : undefined, sendSms, publish: !sendSms && data.get("publish") === "on", confirmSms: false }); form.reset(); }, bn ? "নোটিশ সংরক্ষিত হয়েছে।" : "Notice saved."); }
  return <><header className="portal-page-header"><p className="eyebrow">{bn ? "যোগাযোগ ও শেখা" : "Content and communication"}</p><h1>{bn ? "ম্যাটেরিয়াল ও নোটিশ" : "Materials and notices"}</h1><p>{bn ? "শিক্ষক শুধু নির্ধারিত ব্যাচে প্রকাশ করতে পারেন; SMS সহ নোটিশে প্রিভিউ নিশ্চিত করা বাধ্যতামূলক।" : "Teachers can publish only to assigned batches; SMS notices require an exact recipient preview confirmation."}</p></header><div className="tab-list" role="tablist"><button role="tab" aria-selected={tab === "materials"} onClick={() => setTab("materials")}>{bn ? "ম্যাটেরিয়াল" : "Materials"}</button><button role="tab" aria-selected={tab === "notices"} onClick={() => setTab("notices")}>{bn ? "নোটিশ" : "Notices"}</button></div>{message && <p className="form-message success">{message}</p>}{tab === "materials" ? <><details className="editor-disclosure"><summary>{bn ? "ম্যাটেরিয়াল যোগ করুন" : "Add material"}</summary><form className="operation-form" onSubmit={(event) => void submitMaterial(event)}><fieldset><legend>{bn ? "স্কোপ ও ধরন" : "Scope and type"}</legend><div className="form-grid"><label>{bn ? "কোর্স" : "Course"}<select name="courseId" required>{courses.map((course) => <option key={course.courseId} value={course.courseId}>{bn ? course.nameBn : course.nameEn}</option>)}</select></label><label>{bn ? "ব্যাচ (ঐচ্ছিক)" : "Batch (optional)"}<select name="batchId"><option value="">{bn ? "সম্পূর্ণ কোর্স" : "Whole course"}</option>{batches.map((batch) => <option key={batch.batchId} value={batch.batchId}>{bn ? batch.nameBn : batch.nameEn}</option>)}</select></label><label>{bn ? "ধরন" : "Kind"}<select name="kind"><option value="text">Text</option><option value="link">Link</option><option value="file">File</option></select></label><label>URL<input name="externalUrl" type="url" placeholder="https://" /></label><label>{bn ? "ফাইল" : "File"}<input name="file" type="file" /></label></div></fieldset><fieldset><legend>{bn ? "কনটেন্ট" : "Content"}</legend><div className="form-grid"><label>বাংলা<input name="titleBn" required /></label><label>English<input name="titleEn" required /></label><label>বাংলা<textarea name="descriptionBn" rows={4} /></label><label>English<textarea name="descriptionEn" rows={4} /></label></div></fieldset><label className="check-row"><input name="publish" type="checkbox" /><span>{bn ? "এখনই প্রকাশ" : "Publish now"}</span></label><button className="button button-primary" disabled={busy}>{bn ? "সংরক্ষণ" : "Save"}</button></form></details><div className="status-filter"><button onClick={() => setStatus("draft")} aria-pressed={status === "draft"}>{bn ? "খসড়া" : "Drafts"}</button><button onClick={() => setStatus("published")} aria-pressed={status === "published"}>{bn ? "প্রকাশিত" : "Published"}</button></div>{materials === undefined ? <PortalPageState state="loading" locale={locale} /> : <div className="card-grid">{materials.map((material) => <article className="content-card" key={material._id}><p className="eyebrow">{material.kind} · {material.status}</p><h2>{bn ? material.titleBn : material.titleEn}</h2><p>{bn ? material.descriptionBn : material.descriptionEn}</p>{material.status === "draft" && <button className="button button-secondary" disabled={busy} onClick={() => void execute(() => publishMaterial({ materialId: material._id }), bn ? "প্রকাশিত হয়েছে।" : "Published.")}>{bn ? "প্রকাশ" : "Publish"}</button>}</article>)}</div>}</> : <><details className="editor-disclosure"><summary>{bn ? "নোটিশ তৈরি করুন" : "Create notice"}</summary><form className="operation-form" onSubmit={(event) => void submitNotice(event)}><fieldset><legend>{bn ? "অডিয়েন্স" : "Audience"}</legend><div className="form-grid"><label>{bn ? "ধরন" : "Type"}<select name="audienceType" defaultValue={role === "teacher" ? "batch" : "public"}>{role === "owner" && <><option value="public">Public</option><option value="all_students">All students</option><option value="course">Course</option></>}<option value="batch">Batch</option></select></label><label>{bn ? "কোর্স" : "Course"}<select name="courseId">{courses.map((course) => <option key={course.courseId} value={course.courseId}>{bn ? course.nameBn : course.nameEn}</option>)}</select></label><label>{bn ? "ব্যাচ" : "Batch"}<select name="batchId">{batches.map((batch) => <option key={batch.batchId} value={batch.batchId}>{bn ? batch.nameBn : batch.nameEn}</option>)}</select></label></div></fieldset><fieldset><legend>{bn ? "কনটেন্ট" : "Content"}</legend><div className="form-grid"><label>বাংলা<input name="titleBn" required /></label><label>English<input name="titleEn" required /></label><label>বাংলা<textarea name="bodyBn" rows={6} required /></label><label>English<textarea name="bodyEn" rows={6} required /></label></div></fieldset>{role === "owner" && <label className="check-row"><input name="sendSms" type="checkbox" /><span>{bn ? "অভিভাবক SMS" : "Guardian SMS"}</span></label>}<label className="check-row"><input name="publish" type="checkbox" /><span>{bn ? "এখনই প্রকাশ (SMS ছাড়া)" : "Publish now (without SMS)"}</span></label><button className="button button-primary" disabled={busy}>{bn ? "সংরক্ষণ" : "Save"}</button></form></details><div className="status-filter"><button onClick={() => setStatus("draft")} aria-pressed={status === "draft"}>{bn ? "খসড়া" : "Drafts"}</button><button onClick={() => setStatus("published")} aria-pressed={status === "published"}>{bn ? "প্রকাশিত" : "Published"}</button></div>{notices === undefined ? <PortalPageState state="loading" locale={locale} /> : <div className="notice-list">{notices.map((notice) => <article key={notice._id}><p className="meta-line">{notice.audienceType} · {notice.status}</p><h2>{bn ? notice.titleBn : notice.titleEn}</h2><p>{bn ? notice.bodyBn : notice.bodyEn}</p>{notice.status === "draft" && <>{<div className="form-actions">{notice.sendSms && role === "owner" ? <button className="button button-secondary" aria-expanded={previewId === notice._id} onClick={() => setPreviewId(notice._id)}>{bn ? "SMS প্রিভিউ" : "SMS preview"}</button> : <button className="button button-primary" onClick={() => void execute(() => publishNotice({ noticeId: notice._id, confirmSms: false }), bn ? "প্রকাশিত হয়েছে।" : "Published.")}>{bn ? "প্রকাশ" : "Publish"}</button>}</div>}{previewId === notice._id && preview && <section className="operation-form compact-form" role="alertdialog" aria-labelledby={`notice-sms-title-${notice._id}`} aria-describedby={`notice-sms-detail-${notice._id}`}><h3 id={`notice-sms-title-${notice._id}`}>{bn ? "SMS সহ নোটিশ প্রকাশ নিশ্চিত করুন" : "Confirm notice publication with SMS"}</h3><p id={`notice-sms-detail-${notice._id}`}><strong>{preview.recipientCount}</strong> {bn ? "জন অভিভাবক প্রাপক। প্রকাশ করলে এই নির্দিষ্ট প্রাপক তালিকা স্থির হবে এবং SMS কিউ হবে।" : "guardian recipients. Publishing freezes this exact recipient list and queues the SMS messages."}</p>{!preview.enabled && <p className="form-message error">{bn ? "SMS বর্তমানে নিষ্ক্রিয়; প্রকাশ করা যাবে না।" : "SMS is currently disabled; publication cannot continue."}</p>}<div className="form-actions"><button className="button button-secondary" type="button" disabled={busy} onClick={() => setPreviewId("")}>{bn ? "বাতিল" : "Cancel"}</button><button className="button button-primary" type="button" disabled={busy || !preview.enabled} onClick={() => { void execute(() => publishNotice({ noticeId: notice._id, confirmSms: true, expectedSmsRecipientCount: preview.recipientCount }), bn ? "প্রকাশ ও SMS কিউ হয়েছে।" : "Published and SMS queued.").then((ok) => { if (ok) setPreviewId(""); }); }}>{preview.recipientCount} {bn ? "প্রাপকের জন্য প্রকাশ করুন" : "recipients — publish"}</button></div></section>}</>}</article>)}</div>}</>}</>;
}
